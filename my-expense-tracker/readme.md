# Система аналитики личных трат

Это микросервисное приложение для учета и анализа личных финансов. Особенностью проекта является использование **Google Таблиц** в качестве пользовательского интерфейса (Frontend), в то время как вся бизнес-логика, хранение данных и сложные расчеты выполняются на надежном Backend'е, написанном на **Go**.

![Go](https://img.shields.io/badge/Go-1.21+-00ADD8?style=flat&logo=go)
![Docker](https://img.shields.io/badge/Docker-Compose-2496ED?style=flat&logo=docker)
![gRPC](https://img.shields.io/badge/gRPC-Protobuf-4285F4?style=flat&logo=google)
![PostgreSQL](https://img.shields.io/badge/PostgreSQL-15-336791?style=flat&logo=postgresql)
![Redis](https://img.shields.io/badge/Redis-Cache-DC382D?style=flat&logo=redis)

---

## 🏗 Архитектура проекта

Проект построен на **микросервисной архитектуре** с использованием **gRPC** для межсервисного взаимодействия. Структура папок следует стандарту **Standard Go Project Layout**.

### Структура репозитория

```text
expense-tracker/
├── Makefile                 # Команды автоматизации (запуск, генерация proto, миграции)
├── docker-compose.yml       # Оркестрация контейнеров (App, DB, Redis, UI)
├── .env.example             # Пример переменных окружения
│
├── proto/                   # Единый источник истины для контрактов API (gRPC)
│   ├── auth.proto           # Описание методов сервиса авторизации
│   └── ledger.proto         # Описание методов учета (транзакции, бюджеты)
│
├── gateway/                 # API Gateway (REST -> gRPC)
│   ├── cmd/                 # Точка входа (main.go)
│   └── internal/
│       ├── handler/         # HTTP-хендлеры (принимают JSON от Google Sheets)
│       ├── middleware/      # Проверка JWT токенов
│       └── client/          # gRPC-клиенты для общения с Auth и Ledger
│
├── auth/                    # Микросервис Аутентификации
│   ├── cmd/                 # Точка входа
│   └── internal/            # Внутренняя логика (Clean Architecture)
│       ├── config/          # Чтение конфигурации
│       ├── domain/          # Структуры данных (User)
│       ├── repository/      # Работа с БД (PostgreSQL) и кэшем (Redis)
│       ├── service/         # Бизнес-логика (хеширование паролей, генерация токенов)
│       └── handler/         # gRPC-сервер (реализация интерфейса из proto)
│
└── ledger/                  # Микросервис Учета (Основная логика)
    ├── cmd/                 # Точка входа
    └── internal/
        ├── repository/      # SQL-запросы к транзакциям и бюджетам
        ├── service/         # Логика проверки лимитов, агрегация отчетов
        └── handler/         # gRPC-сервер
```

### Способы запуска

Для запуска сервера:

- Откройте терминал
- Перейдите в корень проекта если еще не там
- Запустите ngrok командой ```.\ngrok.exe http 8080```

Для запуска проекта:

- Откройте терминал
- Перейдите в корень проекта если еще не там
- Запустите контейнеры Postgres, Redis, Auth, Ladger и Gateway командой ```docker-compose up --build -d```

